<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fareez.helpsy">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutNFNActivity"></activity>
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".AdminManageDonationDetailsActivity" />
        <activity android:name=".AdminManageDonationActivity" />
        <activity android:name=".AdminUpdateNewsActivity" />
        <activity android:name=".AdminManageNewsDetailsActivity" />
        <activity android:name=".AdminManageNewsList" />
        <activity android:name=".AccomplishmentActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DonationMapsActivity"
            android:label="@string/title_activity_donation_maps" />
        <activity android:name=".NewsDetailsActivity" />
        <activity android:name=".NewsListActivity" />
        <activity android:name=".AdminPublishNewsActivity" />
        <activity android:name=".AdminUpdateEvent" />
        <activity android:name=".AdminVolunteerList" />
        <activity android:name=".AdminManageEventDetails" />
        <activity android:name=".AdminManageEventListActivity" />
        <activity android:name=".JoinedEventsActivity" />
        <activity android:name=".EventDetailsActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".ProfileSettingsActivity" />
        <activity android:name=".EventListActivity" />
        <activity android:name=".AdminPublishEventActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>